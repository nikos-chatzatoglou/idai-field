{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "6.6.2",
    "@fontsource/roboto": "5.1.1"
  },
  "scripts": {
    "bootstrap": "npm i && lerna bootstrap -- --force",
    "build": "lerna run build"
  }
}
